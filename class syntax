#this is the same as python basically but lil different

class Person
    def initalize(name)
    @name = name #this means an instant variable for the class Person
    #we can create an instance for the class like this
    end
end
matz = Person.new("Samuel|")

    
#ANOTHER ONE

class Message
@@messages_sent = 0 #this works as a class variable and has to come before the initialize
  def initialize(from,to)
  @from = from
  @to = to
  @@message_sent = += 1 # this is how you increment 
  end
end
